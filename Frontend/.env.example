# Base URL of the NestJS backend. The app pings {VITE_API_BASE_URL} itself
# (the Swagger doc's root "App" route) to detect whether the backend is
# attached, and falls back to demo data per-screen if not.
VITE_API_BASE_URL=http://localhost:3000/api
